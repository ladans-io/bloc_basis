name: bloc_basis
description: Flutter BLoC Basis.
version: 2.0.0+2
homepage: "https://ladans.io"

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter:

dependencies:
  equatable:
  flutter:
    sdk: flutter
  rxdart:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
